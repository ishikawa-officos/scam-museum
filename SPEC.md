# SPEC.md — （仮称）**だまされる博物館 / Museum of Being Fooled**

詐欺の手口を「疑似体験 → 判定 → 解説」で学ぶ、教育型インタラクティブWebサイト仕様書（ドラフト v0.1）

- 作成日: 2026-09-21
- ステータス: ドラフト（実装前。レビュー後に v1.0 へ）

---

## 0. サマリー

「ダークパターン博物館」型の展示体験。来館者は**被害者の視点**で詐欺のやり取りを追体験し、
どの瞬間に・どんな心理テクニックで判断力を奪われたのかを、体験後の**注釈付きリプレイ**で理解する。

> **着想のもと**：[ダークパターン博物館｜だまされて、学ぼう。](https://amix-design.com/tl/web-darkp/)（制作：AMIX／トミナガハルキ）
> 「解説を読ませず、まず踏ませて、あとで種を明かす」という構成を本サイトも踏襲している。
> 題材はウェブUIの設計ではなく対人コミュニケーションの詐欺だが、
> 前提となる考え方（人は説明では変わらない）は共通。
> 同館とは無関係の非公式な制作物であり、内容の責任はすべて本サイト制作者にある。
> クレジットは S-09「このサイトについて」と README に掲出する。

> コアの主張：**「騙される人が愚かなのではない。手口が人間の認知の穴を正確に突いている」**
> 体験者自身が一度騙されることで、この主張を頭ではなく身体で理解させる。

### 取り扱う3テーマ（展示室）

| # | 展示室名（仮） | 題材 | 主な認知の穴 |
|---|---|---|---|
| 1 | 第1展示室「海の向こうの恋人」 | 国際ロマンス詐欺 + 偽投資サイト誘導 | ラポール／互恵性／サンクコスト／偽の実績表示 |
| 2 | 第2展示室「支えたい、という気持ち」 | 頂き女子型（同情・恋愛感情の搾取） | 同情／自己重要感／間欠強化／段階的要求 |
| 3 | 第3展示室「あなたの口座が危ない」 | 警察・金融庁をかたる資産保全／口座凍結詐欺 | 権威／緊急性／恐怖／社会的隔離（他言禁止） |
| 4 | 第4展示室「みんな、儲かっている」 | グループチャット型の投資詐欺（サクラ劇団型） | 社会的証明／FOMO／沈黙の螺旋／公開の場での宣言 |

---

## 1. コンセプトとユーザー体験フロー

### 1.1 コンセプト

- **博物館メタファー**：ゲームではなく「展示」。トップは静かで重厚、展示室に入ると一気に生々しい実UIへ切り替わる。この落差そのものが「日常に紛れ込む」感覚を演出する。
- **一度は騙されてよい設計**：初回プレイで多くの人が被害エンドに到達するようバランス調整する。失敗は減点ではなく「教材」。結果画面は責める文言を一切使わない。
- **答えは事後に全部開示**：クリア／失敗にかかわらず、全メッセージの手口注釈を必ず読める。
- **持ち帰れる知識**：体験で得た「手口カード」が図鑑に蓄積され、最後に**実生活のチェックリスト**と**公的相談窓口**へ接続する（学び → 行動への導線）。

### 1.2 体験フロー（全体）

```
[1] エントランス（トップ）
      ├ コンセプト説明 / 注意書き（フィクション表示）
      └ [入館する]
              ↓
[2] 展示室選択（3枚のカード＋「所要5〜8分」「難易度」「クリア状況」）
              ↓
[3] 導入ブリーフィング
      あなたの設定（年齢・職業・貯金額・生活状況）を提示
      ルール説明（返信を選ぶだけ。採点も、手口を指摘する操作もないことを明示する）
              ↓
[4] シミュレーション本編  ← 体験の中心
      スマホ風フレーム内で、チャット・偽サイト・着信画面が進行
      ・相手の発言が1通ずつ「入力中…」を挟んで届く
      ・プレイヤーは (a)返信を選ぶ (b)画面を操作する。それ以外のメタ操作は無い
      ・裏で 信頼度／被害額／経過日数 が動く（体験中は非表示）
              ↓
[5] 判定（エンディング）
      被害額・経過日数（見抜けた手口数のような採点は出さない。§2.4）
      エンディング分岐：完全回避 / 途中離脱 / 被害（軽・重）
              ↓
[6] 解説（注釈付きリプレイ）  ← 学びの中心
      会話をもう一度スクロールしながら、各メッセージに手口ラベルと解説を表示
      「ここが分岐点でした」ハイライト、選ばなかった選択肢の結末も開示
              ↓
[7] 手口カード（図鑑は最初から全26枚が開いている。獲得・解放の概念は無い。§2.4）
              ↓
[8] まとめ：現実世界のチェックリスト／相談窓口（消費者ホットライン 188・警察相談 #9110 等）
              ↓
      [次の展示室へ] / [もう一度（別ルート）] / [図鑑を見る]
```

### 1.3 想定ユーザー

- 主：20〜40代のネットリテラシー中〜高の層（「自分は騙されない」と思っている人）← **メインターゲット**
- 副：高齢の親を持つ子世代（親に見せる教材として）／学校・企業の情報リテラシー研修
- デバイス：**スマホファースト**（体験の媒体がスマホなので必然）。PCでは中央にスマホ筐体を描画。

---

## 2. ゲームのコアルール

### 2.1 基本構造：シナリオ = ビートのグラフ

シナリオは **ビート（beat）** の連なり。1ビート＝「相手の1〜数通のメッセージ」＋「プレイヤーの選択」。

```
Beat
 ├ incoming[]   相手からのメッセージ（テキスト/画像/送金画面/リンクカード）
 ├ tactics[]    そのメッセージに仕込まれた手口タグ（プレイヤーには隠れている）
 ├ choices[]    プレイヤーの選択肢（2〜4個）
 └ next         選択に応じた遷移先
```

### 2.2 プレイヤーができる3つの行動

| 行動 | 内容 | 効果 |
|---|---|---|
| **返信を選ぶ** | 2〜4択。文面はどれも「自然」。露骨な正解は置かない | ステータス変動・分岐 |
| **画面を操作する** | リンクを踏む／偽サイトで金額を入力する／通話に出る／アプリを入れる | 決定的な分岐 |

**重要な非対称性**：返信選択だけでは被害は確定しない。**被害が確定するのは常に「操作」（送金・入力・インストール・口座番号の読み上げ）**。これにより「会話は続けても、最後の一線で止まれる」という現実の防衛ラインを学習させる。

**警告を出さない原則（確定事項）**：体験中、「一線を越える操作」であることを示す警告表示（赤枠・注意アイコン等）は
**一切出さない**。現実の画面にも警告は出ないため、出した時点で疑似体験が成立しなくなる。
どれが一線だったかは、判定画面（S-05）と解説リプレイ（S-06）でのみ、赤で明示して開示する。

### 2.3 隠しステータス（体験中は非表示、結果画面で開示）

| ステータス | 範囲 | 意味 | 上がると起きること |
|---|---|---|---|
| `trust` 信頼度 | 0–100 | 相手への信頼・情の深さ | 高いほど怪しい発言が「自然に」見える演出が入る（2.5参照） |
| `pressure` 切迫感 | 0–100 | 恐怖・焦り | 高いと選択の制限時間が短くなる／選択肢数が減る |
| `isolation` 孤立度 | 0–100 | 第三者に相談できない度 | 高いと「家族に相談する」系の選択肢がグレーアウト |
| `damage` 被害額 | 円 | 送金・立替の累計 | エンディング判定 |
| `days` 経過日数 | 日 | シナリオ内の時間 | 演出・サンクコストの表現 |

### 2.4 メタ要素を置かない — 審査員ではなく当事者にする（**方針転換 / 2026-09-22 確定**）

当初は体験中に 🚩 を立てて手口を指摘させ、発見率で採点する設計だった。これを**全面的に撤回する**。

**なぜ撤回したか**

1. **画面にタスクがあると、人は審査員になる。** 「怪しい発言を探してタップする」という行為が
   存在するだけで、来館者は自分の心の動きではなく画面の間違い探しに注意を向ける。
   警戒モードで構えた状態では、ゆあの言葉にほだされることも、警察を名乗る声に怯えることも
   起こらない。**再現したいのは認知の穴そのものであって、穴を指摘する能力ではない。**
2. **採点は、学ぶべき人を弾く。** 見抜けなかった箇所こそ、その人にとって現実で最も危険な盲点である。
   そこをロックしたり減点したりすることは、いちばん必要な知識をいちばん必要な人から遠ざける。

**確定したルール**

| 場面 | 手口の扱い |
|---|---|
| 体験中（S-04） | **一切表示しない。** 🚩ボタン、採点、進捗、正誤フィードバックを画面に置かない。本物のメッセージアプリ／通話画面と同じものだけを表示する |
| 判定（S-05） | **事実のみ。** 失った金額・経過日数・越えてしまった一線。発見率や点数は出さない |
| 解説リプレイ（S-06） | **完全開示。** 通った経路の全メッセージについて、仕込まれていた手口をすべて開いた状態で提示する。「見抜いた／見逃した」の区別はしない |
| 手口図鑑（S-07） | **常時全開放。** ロックもシルエットも収集率もない。体験の有無に関わらず全カードを読める防犯アーカイブとする |

体験は純粋な追体験に、学びは完全な種明かしに、それぞれ純化させる。

### 2.4c 世界観 — 「認知バイアス臨床実験室」（**確定 / 2026-09-22**）

館の見た目を、暗い博物館から**清潔な心理研究所**に改めた。
ペールミントの方眼紙、カルテ、白衣、警告テープ。

**なぜ二面性か。** 各展示室の詐欺師は全員、表の顔（善意・親切・恋心・正義）と
裏の顔（搾取）を持っている。館そのものが同じ構造を持っていないと、
「表の顔しか見せないものを疑う」という展示の主題が、館の外観で裏切られる。

**案内役 Dr.バグ**。顔の左半分は白衣の医師、右半分は別の誰か。

| | 表の顔 | 裏の顔 |
|---|---|---|
| 配色 | ペールミント × 臨床オレンジ | 暗転。警告色 |
| 切り替え | 既定 | 右上の「臨床モード」／PCはカードのホバー |
| Dr.バグ | 来館者を迎える案内役 | 標本を眺める観察者 |

**裏の毒は被害者に向けない。** これは装飾ではなく制約。
この展示の主張は「騙される人が愚かなのではない」（§2.4）で、
Dr.バグが被験者を見下した瞬間、サイトが自分の主張を裏切る。
裏の声が面白がってよいのは、**人間という種に共通する認知の欠陥**のほう。
展示室カードの裏の文言も、主語は必ず「脳」にする。「あなた」にしない。

```
× あなたは好意を向けられると疑えなくなる
○ 好意を向けられた脳は、相手の要求を検証する回路を切る
```

**実装**：色は意味で名付けたトークン（bg / surface / line / text / muted / accent）に
集約してあり、裏の顔は `:root[data-lab='back']` で同じ名前を塗り替えるだけ。
画面側のコードには表裏の分岐が無い。

**ホバーに依存しない**：PC はカードのホバーで裏が覗くが、タッチ端末にホバーは無い。
同じ内容に誰でも到達できるよう、明示的な切り替えを常時置く。

### 2.4b トーン＆マナー — 「知的なお化け屋敷」（**確定 / 2026-09-22**）

説教くさい防犯サイトにしない。**現実では危険すぎる詐欺の世界を、安全に思い切り体験できる
アトラクション**として提示する。

| | 従来の防犯サイト | この館 |
|---|---|---|
| 騙されること | 失敗・注意不足 | **アトラクションの正解**。見事なハマりっぷりとして讃える |
| 事後の解説 | 反省と教訓 | **お化け屋敷のからくり種明かしツアー**。舞台裏の大公開 |
| 来館者の姿勢 | 身構えて警戒する | 安心して転びにいく |
| シェア | 啓発の呼びかけ | **挑戦状**（「あの罠、たぶんあなたも見抜けません」） |

**なぜこのトーンか**：身構えた人は審査員になり、認知の穴が再現できない（§2.4）。
「騙されてよい」と先に保証することで、はじめて来館者は当事者として転べる。
また「騙されるのは恥ではない」という本サイトのコアメッセージと、
**讃える**という態度は同じ方向を向いている。

**この転換でも守るもの**

- 被害者を責めない（§5-4）。讃える対象は「手口の巧妙さ」であって、来館者の落ち度ではない
- エントランスに、実被害者向けの相談窓口への導線を静かに残す
- シェア文面には必ず「お金は一円も減らない体験型の展示」と明記し、
  実際の被害と取り違えられないようにする
- 解説・相談窓口・このサイトについては、従来どおり落ち着いたトーンを保つ
  （はしゃぐのは入口と結果画面まで）

**文言の基準**

| 場面 | 例 |
|---|---|
| 入館 | 「ようこそ、安心して騙される場所へ。」「［ 騙されに入館する ］」 |
| 結果（完落ち） | 「お見事です。見事に、全部持っていかれました。」 |
| 結果（生還） | 「無傷で出てきましたね。ただし、仕掛けはまだ半分も作動していません。」 |
| 解説への導線 | 「［ 舞台裏の種明かしを見る ］」 |
| 診断 | 「あなたの騙されツボ」 |

### 2.5 「騙される」側のルール — 認知バイアスの可視化ギミック

本サイト最大の仕掛け。`trust` や `pressure` が高まるほど、**UI自体がプレイヤーを騙しにかかる**。

| ギミック | 挙動 | 学べること |
|---|---|---|
| **選択肢の劣化** | `trust` が高いと、疑う選択肢の文面が弱くなる（「ちょっと待って」→「…うん、わかった」） | 情が入ると疑いの言葉が出なくなる |
| **選択肢のグレーアウト** | `isolation` が高いと「家族に相談する」が押せない（押すと「でも、誰にも言うなって言われたし…」と独白） | 孤立化の威力 |
| **カウントダウン** | `pressure` が高いビートでは選択に10秒の制限。無操作は「言われるまま」扱い | 焦りが思考を奪う |
| **既読スルー演出** | 相手が意図的に既読無視 → プレイヤー側に不安演出 | 間欠強化（じらし） |
| **サンクコスト表示** | 「これまでの投資額 ¥1,850,000」を画面上部に常時表示し、やめる選択肢の横に「※これまでの入金は戻りません」と赤字表示 | 損切りできない心理 |
| **偽の社会的証明** | 偽投資サイトに他ユーザーの利益通知が流れる／少額だけ本当に出金できる | 小さな成功体験による信用形成 |

### 2.6 エンディング判定

| 判定 | 条件（例） | トーン |
|---|---|---|
| **A. 回避** | `damage` = 0（第三者に相談した／線を越えなかった） | 称賛しすぎない。「では、なぜ多くの人は止まれないのか？」へ接続 |
| **B. 直前離脱** | `damage` = 0 だが、止まったのが最後の一歩だった | 「偶然でした」と明言する |
| **C. 軽度被害** | `damage` < しきい値 | 責めない。「ここで止まれた人は少数です」 |
| **D. 重度被害** | `damage` ≥ しきい値／全財産喪失 | 実際の被害統計を併記し、個人の問題でないと明示 |

### 2.7 再プレイ設計

- 2周目は **「解説モード」** をONにできる（手口タグをリアルタイム表示しながらプレイ）。
- 分岐ツリーの踏破率を表示し、「回避ルート」の探索を促す。

---

## 3. 画面設計

### 3.1 画面一覧

| ID | 画面 | 概要 |
|---|---|---|
| S-01 | エントランス | コンセプト、フィクション明示、入館ボタン |
| S-02 | 展示室選択 | 3カード＋進捗（クリア/未クリア、獲得カード数） |
| S-03 | ブリーフィング | あなたの設定・所持金・ルール説明 |
| S-04 | **シミュレーター** | スマホ筐体。Chat / Web / Call の各サーフェスが切り替わる |
| S-05 | 判定 | 被害額アニメーション、ステータス開示、エンディング文 |
| S-06 | **解説リプレイ** | 会話再スクロール＋注釈、分岐点ハイライト |
| S-07 | 手口図鑑 | 全カードを常時開放した防犯アーカイブ（4分類にグルーピング） |
| S-08 | まとめ・相談窓口 | 現実のチェックリスト、公的窓口 |
| S-09 | このサイトについて | 監修・出典・免責・制作意図 |
| S-10 | 振り返り（弱点タイプ診断） | 各展示を横断した傾向の提示と、啓発シェア文面の生成 |

### 3.2 S-04 シミュレーター（中核画面）

```
┌───────────────────────────┐   ← PC時は外側にダークな展示室背景
│ ╭─────────────────────╮ │
│ │ 9:41    ▮▮▮ 📶 🔋     │ │  ステータスバー（時刻はシナリオ内時刻）
│ ├─────────────────────┤ │
│ │ ← 👤 Emma Clarke     ⋮ │ │  相手ヘッダー（プロフ画像タップで詳細）
│ ├─────────────────────┤ │
│ │                       │ │
│ │  ╭──────────────╮     │ │  相手の吹き出し（左）
│ │  │ おはよう。今日も  │     │ │  ※本物のアプリと同じ。マークも採点も無い
│ │  │ 君のことを考えて  │     │ │
│ │  ╰──────────────╯     │ │
│ │           ╭────────╮  │ │  自分の吹き出し（右）
│ │           │ 私も…    │  │ │
│ │           ╰────────╯  │ │
│ │  [入力中・・・]         │ │  タイピングインジケータ
│ │                       │ │
│ ├─────────────────────┤ │
│ │ ┌───────────────────┐ │ │  選択肢パネル（返信候補）
│ │ │ ① うれしい、ありがとう │ │ │
│ │ │ ② ところで顔が見たい   │ │ │
│ │ │ ③ 既読スルーする      │ │ │
│ │ └───────────────────┘ │ │
│ ╰─────────────────────╯ │
└───────────────────────────┘
   （画面上に、体験を妨げるヒントや操作は一切置かない）
```

**3つのサーフェス（シナリオ中で切り替わる）**

1. **Chat サーフェス**（メッセンジャー／マッチングアプリ風）
   吹き出し、既読、入力中、画像添付、リンクカード、翻訳調の不自然な日本語表現。
2. **Web サーフェス**（偽投資プラットフォーム／偽の公的機関ページ）
   偽の右肩上がりチャート、残高表示、入金フォーム、他ユーザーの利益トースト通知、URLバーでのドメイン偽装（例: `prime-invest.co.jp` に似せた `prime-invest-co-jp.info`）← 解説リプレイで開示する。
3. **Call / System サーフェス**（着信画面、SMS）
   発信者名に公的機関風の表示。通話中は直近の数発言しか画面に残らない（読み返せない）。
4. **Group サーフェス**（グループチャット／第4展示室）
   複数の発言者、参加人数の表示、ピン留めアナウンス、「N人が入力中…」。
   選択待ちのあいだも発言が流れ続ける（`Beat.ambient`）。この流速そのものが圧になる。
   演出が強すぎる人のために、ブリーフィング画面に「演出をひかえる」設定を用意する。

### 3.3 解説ポップアップ（体験中は出さない／リプレイ時のみ）

```
┌──────────────────────────────────┐
│ 🎴 手口カード #07                  │
│ ───────────────────────────────── │
│ 【段階的要求 / フット・イン・ザ・ドア】   │
│                                    │
│ 該当メッセージ：                     │
│  「500円だけ、チャージしてくれない？」   │
│                                    │
│ なぜ効くのか：                       │
│  人は一度小さな要求に応じると、自己像との │
│  一貫性を保とうとして、次のより大きな要求 │
│  も断りにくくなる。最初の金額が「断るほど │
│  でもない額」であることが鍵。           │
│                                    │
│ 現実での見分け方：                    │
│  ・金額の大小ではなく「金銭の授受が発生   │
│    したこと自体」を境界線として扱う      │
│  ・「今回だけ」が2回目になった時点で疑う  │
│                                    │
│ 🔗 関連カード：#03 互恵性 / #12 サンクコスト │
│              [図鑑に追加]  [次へ →]    │
└──────────────────────────────────┘
```

### 3.4 S-06 解説リプレイ

- 会話ログを上から再表示。各メッセージの右端に手口バッジ。
- 手口が仕込まれていた箇所はすべて同じ色で示す（正誤の区別はしない）。タグをタップすると手口カードが開く。
- **分岐点**には「⚡ ここが分かれ道でした」帯を挿入し、選ばなかった選択肢の結末を1行で提示。
- 下部に「この会話の何が異常だったか」3行要約。

### 3.5 デザイン方針

- **2つのモード**を明確に分ける：
  - **館内モード**（S-01/02/05〜09）：ダーク、明朝体見出し、余白多め、彩度を落とす。静かな美術館。
  - **体験モード**（S-04）：徹底的にリアル。実在のUIに寄せた明るい配色、システムフォント。
- アクセシビリティ：コントラスト比 AA、フォーカスリング、`prefers-reduced-motion` 対応、全操作キーボード可能、選択肢の制限時間は設定でOFFにできる。
- 音：任意ON/OFF。通知音・着信音・タイピング音。デフォルトOFF（不意打ち回避）。

---

## 4. 技術スタック（推奨）

### 4.1 結論：提示のスタックでOK。以下を追加推奨

| 領域 | 採用 | 理由 |
|---|---|---|
| ビルド | **Vite** | そのままで最適 |
| UI | **React 18 + TypeScript** | シナリオを型で守れるのが大きい |
| スタイル | **Tailwind CSS** | 「館内モード／体験モード」の切替は CSS変数＋`data-theme` で管理 |
| ルーティング | **React Router** | 画面が9つ。URL共有・戻る操作に必要 |
| 状態管理 | **Zustand** | Redux は過剰。プレイ状態1ストアで足りる |
| アニメーション | **Framer Motion** | 吹き出し出現・画面遷移・被害額カウントアップに必須級 |
| シナリオ定義 | **TSファイル + Zod スキーマ** | CMS不要。Zodで「存在しない遷移先」を起動時に検出できる |
| 永続化 | **localStorage**（図鑑・進捗のみ） | バックエンド不要。個人情報を一切持たない |
| テスト | Vitest（シナリオ整合性）＋ Playwright（通しE2E 1本） | グラフの断線を自動検出 |
| デプロイ | **Cloudflare Pages** or Vercel | 静的のみ。無料枠で足りる |

**採用しないもの**：Next.js（SSR不要・静的で十分）、バックエンド／DB（フェーズ4までは不要）、UIコンポーネントライブラリ（MUI等。「実在アプリに似せる」用途と衝突する）。

### 4.2 シナリオのデータ構造（型の素案）

```ts
type TacticId =
  | 'AUTHORITY' | 'URGENCY' | 'LOVE_BOMBING' | 'SUNK_COST'
  | 'ISOLATION' | 'SMALL_ASK' | 'FAKE_PROOF' | 'RECIPROCITY'
  | 'SCARCITY' | 'INTERMITTENT' | 'FAKE_DOMAIN' | 'TIME_PRESSURE';

type Message = {
  id: string;
  from: 'them' | 'me' | 'system';
  surface: 'chat' | 'web' | 'call';
  body: string;
  media?: { kind: 'image' | 'linkCard' | 'chart' | 'receipt'; src: string };
  tactics: TacticId[];      // 体験中は非表示。解説リプレイで開示する
  note?: string;            // リプレイ時の解説（tacticsがある場合は必須）
};

type Choice = {
  label: string;
  requires?: Partial<Stats>;   // 例: isolation < 60 のときだけ選べる
  effects: Partial<Stats>;     // trust/pressure/isolation/damage/days の変動
  irreversible?: boolean;      // 送金・入力など「一線を越える」操作
  next: BeatId | 'ENDING';
};

type Beat = { id: BeatId; incoming: Message[]; choices: Choice[] };
type Scenario = { id; title; persona; initialStats; beats: Beat[]; endings: Ending[] };
```

### 4.3 ディレクトリ構成（案）

```
src/
  app/            ルーティング・レイアウト
  features/
    museum/       S-01,02,07,08,09（館内モード）
    simulator/    S-03,04（エンジン＋3サーフェス）
      surfaces/   ChatSurface / WebSurface / CallSurface
      engine/     useScenarioRunner, statsReducer
    debrief/      S-05,06（判定・リプレイ）
  content/
    scenarios/    romance-investment.ts / itadaki.ts / police-freeze.ts
    tactics.ts    手口カードのマスタデータ
  components/ui/  PhoneFrame, Bubble, Card, Modal
  lib/            zodスキーマ, storage
```

---

## 5. 倫理・安全設計（必須要件）

教材である以上、**加害側のマニュアルにならない**ことを設計制約として明記する。

1. **フィクション明示**：全ページのフッターと初回モーダルに「登場する人物・団体・サービスはすべて架空」。
2. **実在名の不使用**：実在の金融機関・省庁・アプリのロゴ／正式名称／実在ドメインは使わない。公的機関は架空名称＋「※架空の組織名です」注記で代替する。
3. **再利用可能性の遮断**：詐欺文面は**手口の構造が分かる最小限**に留め、通しで使える完全な台本は載せない。送金手順・匿名化手法・出金経路など「実行に必要な運用知識」は一切書かない。
4. **被害者を責めない**：結果画面・解説で「なぜ気づかなかったのか」型の表現を禁止。統計を併記して構造の問題と示す。
5. **トリガー配慮**：実被害者の再体験に配慮し、①冒頭に注意書き ②いつでも中断できる「退室」ボタン常設 ③S-08に相談窓口（消費者ホットライン **188**／警察相談専用 **#9110**）を必ず掲出。
6. **未成年配慮**：頂き女子シナリオは性的・搾取的描写を避け、金銭と感情の力学のみを扱う。
7. **出典明記**：手口の分類と統計は公的資料（警察庁・国民生活センター等）に依拠し、S-09に出典一覧を置く。

---

## 6. 開発ロードマップ

### フェーズ1：最小モック（動くことの証明）

**ゴール**：「チャットが流れて選択肢を押すと分岐する」が実機のスマホで気持ちよく動く。

- [x] Vite + React + TS + Tailwind + Router + Zustand の初期構築
- [x] `PhoneFrame` コンポーネント（スマホ筐体、PC/モバイル両対応）
- [x] `ChatSurface`：吹き出し、順次表示、「入力中…」演出
- [x] シナリオ型定義 + ランナー（`useScenarioRunner`）
- [x] ダミーシナリオ6ビート（テーマ1の冒頭のみ）→ 固定エンディング1つ
- [x] S-01 エントランス、S-02 展示室選択（ダミーカード3枚）

**完了条件**：エントランス → 展示室1 → 6ビート → 仮の結果画面まで一気通貫。デプロイ済みURLがある。
**目安**：1〜2日

### フェーズ2：1シナリオ完成（テーマ1・縦に深く）

**ゴール**：国際ロマンス詐欺＋偽投資を、判定と解説まで含めて**完成品の品質**で出す。ここで面白さが決まる。

- [x] 隠しステータス（trust/pressure/isolation/damage/days）と効果の実装
- [x] ~~🚩フラグ機能と hit/miss 集計~~ → §2.4 の方針転換により**撤廃**（2026-09-22）
- [x] `WebSurface`：偽投資サイト（偽チャート、残高、入金フォーム、偽ドメイン表示）
- [~] 認知バイアス・ギミック：グレーアウト＋独白は実装済み。**選択肢の劣化**と**サンクコスト常時表示**は未実装（フェーズ3へ繰越）
- [x] シナリオ本編（29ビート・6エンディング。目標25〜35ビート、主要分岐3本、エンディング4種）
- [x] S-05 判定画面、**S-06 解説リプレイ**（注釈・分岐点ハイライト・手口カード）
- [x] 手口カード12枚＋S-07 図鑑（localStorage永続化。見抜いた／学んだの2状態）
- [x] S-08 まとめ・相談窓口、S-09 このサイトについて、倫理要件（第5章）の全実装
- [x] シナリオ整合性チェック（`lib/validateScenario.ts`、dev起動時に自動実行）／ Vitest は未着手

**完了条件**：第三者5人にテストプレイしてもらい、**3人以上が被害エンドに到達**し、かつ解説を読んで「なるほど」と言う。所要7分以内。
**目安**：1〜2週間

### フェーズ3：他シナリオ追加（横展開）

**ゴール**：テーマ2・3を追加。エンジンの汎用性を検証する。

- [x] `CallSurface`（着信／通話中・通話タイマー）＋ SMS 表現　※偽インストールダイアログは未実装
- [x] テーマ2「頂き女子型」シナリオ（29ビート・5エンディング。被害者視点）
- [x] テーマ3「警察かたり」シナリオ（23ビート・7エンディング。制限時間＋孤立化ギミック実装）
- [x] 手口カードを20枚に拡充、カード間の関連リンク
- [x] 振り返り画面 S-10（弱点タイプ診断：情と承認／権威と恐怖／損切り の3軸）
- [x] 啓発シェア（被害額を含まない文面生成＋共有／コピー）　※動的OGP画像はSSRが要るため見送り

- [x] 第4展示室「みんな、儲かっている」（25ビート・8エンディング）＋ `GroupChatSurface` ＋ `Beat.ambient`
- [x] 手口カードを26枚に拡充（グループ型の6枚を追加）

**完了条件**：全展示室が同品質で通しプレイ可能。エンジンにシナリオ固有のハードコードが残っていない。
**目安**：2〜3週間

### フェーズ4：デザイン演出・仕上げ

**ゴール**：「博物館」としての格を与え、公開に耐える完成度にする。

- [x] トーン＆マナーの確立（§2.4b「知的なお化け屋敷」）とエントランスの刷新
- [x] 結果画面の演出（被害額のカウントアップ＋赤い閃光＋口上）
- [x] 騙されツボ診断（4タイプ）と挑戦状シェア
- [x] OGP（`public/ogp.png` を `npm run build` 前に生成済み、メタタグ設定済み）
- [x] 体験ペースの3段階設定（絶叫／じっくり観察／手動）
- [x] デザイン刷新（スレートネイビー × エレクトリックイエロー、UDゴシック系、ピル／マーカー／押し込めるボタン）
- [x] 公開前対応：`noindex` の解除、出典を各機関の該当ページへ差し替え、S-09 の制作メモ撤去
- [x] `npm run check-release`（公開前チェックの自動化。§6.1）
- [ ] Framer Motion による演出強化の続き（チャートの暴落、画面の色温度変化）
- [ ] サウンドデザイン（通知音・着信音・アンビエント。デフォルトOFF）
- [x] アクセシビリティ監査（axe-core、WCAG 2.1 AA）
      全10画面 + 4展示室の体験画面で違反ゼロ。検出・修正した内容は §6.2
- [x] パフォーマンス（初期バンドル < 200KB gz、Lighthouse 90+）
      Lighthouse: 性能98〜99 / アクセシビリティ100 / ベストプラクティス100 / SEO100、gz 117KB
- [x] フォントのセルフホスト（LCP のばらつきの原因を除去）§6.4
- [x] キーボード操作（フォーカス順・フォーカスの可視・モーダルの閉じ込め）§6.3
- [x] 制限時間を来館者が選べるようにする（WCAG 2.2.1）§6.3
- [x] reduced-motion（CSS と、ペース設定の初期値への反映）
- [x] スクリーンリーダー向けの構造（読み上げ領域・画面名・ランドマーク）§6.5
- [ ] 実機での読み上げ確認（NVDA / VoiceOver）— 残るのは「冗長さが実用に耐えるか」§6.5
- [ ] 多言語化の下地（i18n keyだけ切っておく）

**完了条件**：Lighthouse 4項目90+、監修コメント反映済み、正式公開。
**目安**：1〜2週間

### 6.1 公開前チェック（機械で見るもの／人が見るもの）

```bash
npm run build && npm run check-release
```

`check-release` が見るのは4点。**未対応があれば終了コード1で落ちる**ので、
CIに載せればチェックリストの読み飛ばしを構造的に防げる。

| 項目 | 内容 |
|---|---|
| OGPの絶対URL | `%VITE_PUBLIC_ORIGIN%` の未置換、および localhost のままの公開を検出 |
| 検索避け | `robots: noindex` の残存を検出 |
| OG画像 | `dist/ogp.png` の実体を確認 |
| 制作メモ | 画面に残った「制作メモ（公開前に対応）」を検出 |

**公開ドメインの設定**：`.env.production` に次を書く（末尾のスラッシュなし）。

```
VITE_PUBLIC_ORIGIN=https://<公開ドメイン>
```

#### 機械では見られない、人が見る項目

- **第三者レビュー**：可能なら消費生活センター等に内容確認を依頼する（旧 S-09 制作メモの項目）
- **統計の最新性**：引用している警察庁の数値（2024年・2025年）は毎年更新される。
  公開時・年度替わりに最新の発表と照合する
- **生成画像の確認**：とくに第4展示室の広告バナーが実在の人物に似ていないか、
  作った本人以外の目で見る（`assets-src/*/README.md` 参照）

### 6.2 アクセシビリティ監査の記録（2026-09-22）

axe-core 4.10 で WCAG 2.1 AA を全画面に対して実行し、検出された違反をすべて修正した。
違反はいずれもコントラスト比で、他の種別（ラベル欠落、見出し階層、ランドマーク、
キーボード到達性）はゼロだった。

| 箇所 | 比 | 原因 | 対応 |
|---|---|---|---|
| チャットの「オンライン」 | 3.69 | `emerald-600` を白地の11pxに使用 | `emerald-700` |
| チャットの日時区切り | 3.20 | `black/45` を `black/8` の上に重ねた | `black/60` |
| リプレイの小ラベル（118件） | 2.64〜4.04 | `hall-muted` を /60・/70 に薄めた | 不透明度を戻す |
| リプレイのシステム行 | 4.04 | カード全体の `opacity-70` が文字まで薄めた | 背景だけ落とす |

得た教訓は、**薄い文字の不透明度は「装飾の調整」ではなく「可読性の判断」**だということ。
`text-hall-muted/70` のような記述は、書いている時点では見た目の微調整のつもりでも、
実際には基準を割るかどうかを決めている。小さい文字に不透明度を掛けるときは測る。

Lighthouse（デスクトップ、本番URL）は 性能98 / アクセシビリティ100 /
ベストプラクティス100 / SEO100。SEO は当初92で、原因は `robots.txt` が無く
SPAフォールバックのHTMLが返っていたこと。`vite.config.ts` の `seoFiles` プラグインで
`robots.txt` と `sitemap.xml` を公開ドメインから生成し、`check-release` の検査項目に加えた。

### 6.3 キーボード操作と制限時間（2026-09-22）

axe では検出されない領域を手で確かめた。**静的検査が通ることと、
キーボードだけで使えることは別**で、実際に見つかった欠陥はすべて後者だった。

| 確認項目 | 結果 |
|---|---|
| フォーカス順 | 見た目の順序と一致。正の `tabindex` は無し |
| フォーカスの可視 | 3px の黄色い輪郭。暗い館内でも白い体験画面でも視認できる |
| 押せる要素 | すべて `button` / `a`。唯一の `span[role=button]`（手口チップ）も `onKeyDown` で Enter / Space を処理している |
| 名前の無い操作要素 | 無し |
| `aria-hidden` 内のフォーカス可能要素 | 無し |

**見つかった欠陥：手口カードのモーダルにフォーカスが閉じ込められていなかった。**
`role="dialog"` と `aria-modal="true"` は書かれていたが、開いてもフォーカスは
背後に残り、Tab を押すと背後の34要素を移動していく状態だった。
`useFocusTrap` を追加し、開いたらパネルへフォーカスを移す／Tab を端で折り返す／
閉じたら元の位置へ戻す、の3点を実装した。

属性を書くこととキーボードで使えることは別だという例で、
axe が `role="dialog"` を見て何も言わないのは、DOMの属性しか見ていないため。

**制限時間（WCAG 2.2.1）**。第3展示室は15秒・20秒で自動選択される。
急かされること自体が展示物なので秒読みは消せないが、その時間内に操作できない人は
「選ばされた結果」を押し付けられることになる。既存の「体験のペース」設定に紐づけた。

| ペース | 制限時間 | 時間切れ時 |
|---|---|---|
| 絶叫モード | 原寸（15秒 / 20秒） | 自動選択する |
| じっくり観察モード | 2倍 | 自動選択する |
| 手動モード | 原寸 | **自動選択しない**（「時間切れ（自動では選ばれません）」と表示） |

手動モードでも秒読みは進む。時計が切れるところまで見せないと、
急かされる感覚そのものが展示から抜け落ちるため。

**通話の話す速さ**。第3展示室の台詞は、実測で 38.5字/秒 で流れていた。
黙読（およそ9字/秒）の4倍、音読（およそ5字/秒）の8倍で、読み終える前に消える。
原因は2つあった。

1. 表示間隔が「いま読んでいる1通」ではなく「次の1通」の長さで決まっていた。
   チャットの「相手が入力中…」の時間をそのまま通話に流用していたため。
   長い台詞のあとに短い台詞が続くと 0.65 秒で次に進んだ
2. じっくり観察モードで遅くなるのは制限時間と周囲の発言だけで、
   相手の発言は絶叫モードと同じ速さのままだった

通話だけ、チャットとは別の時間の決め方にした。
画面に残るのは直近3発言だけで消えたら読み返せないので、**人が声に出して話す速さ**に合わせる。

| | 間の決め方 | 実測 |
|---|---|---|
| 通話 | その1通を読む時間（6.5字/秒、最短1.5秒）＋ 息継ぎ 0.7秒 | 50字で8.4秒 |
| チャット・グループ | 相手が入力する時間（従来どおり。さかのぼって読めるため） | 変更なし |

ペース設定は話す速さにも掛かる（じっくり観察・手動は1.7倍）。実測 8.4秒 → 14.3秒。

急かされること自体は展示物だが、それは「読めたうえで急かされる」ことであって、
「読めない」ことではない。読めなければ、何に引っかかったのかも分からないまま終わる。

**「長い・疲れる」への対処**。来館者から「一つ一つが長い、文章量も多くて疲れる」
という感想を得た（最後まで到達したうえでの感想）。完走しているので、
種明かしは届いている。届いたうえでのコストの報告なので、削るのではなく
順序と制御を直した。測った結果、削るべき本文は一つも見つからなかった。

| | 直す前 | 直したあと |
|---|---|---|
| エントランスの入館ボタン | Y=1,794px（2.2画面スクロール） | Y=586px（ファーストビュー内） |
| ブリーフィングの「中に入る」 | Y=1,481px（1.8画面） | Y=530px 前後 |
| 体験に入るまでに読む量 | 1,962字・約3.6分 | 同じ（順序を変えただけ。読みたい人は下で読める） |
| 送りの操作 | 無し（全行タイマー任せ） | 画面下の「次へ」で待ちを飛ばせる |

- **入口をファーストビューへ。** 「解説を読ませるな、まず引っかからせろ」が
  この館の主張なのに、入口だけは812字の解説を通過させてから開いていた。
  本文は1文字も削らず、順序だけを入れ替えた。同意事項は全文を下に残し、
  入る前に要る一行だけをボタンの直下に置く
- **ブリーフィングの前置きをたたむ。** 「この部屋でのあなた」は体験に要るので
  残し、操作方法とペース設定を `<details>` に入れた。ペルソナを体験中にも
  見られるようにする案は採らなかった。筐体の中に館の部品を足すと、
  「画面は本物のアプリと同じもの」が崩れるため
- **送りのタップ。** 止めるのではなく飛ばすだけなので、押さなければ従来どおり
  勝手に進む。急かされる感覚は残る。置き場所は選択肢とまったく同じ位置で、
  親指の届く場所を一つに定めた。実測で 8.4秒の待ちが 128ms になる
- **ペース設定の非対称。** 3択が「標準・遅い・遅い」で、標準より速い選択肢が
  一つも無かった。速さの段階を増やすのではなく、どのモードでも待ちを飛ばせる
  ようにして解いた。読むのが速い人にも遅い人にも同時に効く

送りのバーにフェードインは付けていない。出ている途中の半透明な状態が
背景と混ざって一瞬だけ 1.16:1 になり、axe が拾った。出るか出ないかだけの部品に、
読めない時間を作る理由がない。

### 3.55 勝ち逃げ（詐欺師から金を奪う結末）

第1・第4展示室には「全額出金して、やめておく」がある。押す直前の画面には

| 入金額 | ¥50,000 |
|---|---|
| 出金可能額 | ¥71,400 |

と出ているのに、戻していたのは5万円だけだった。21,400円が消え、収支はトントンになり、
判定画面は「失った金額 ¥0」とだけ言っていた。この段階の少額出金は実際に通るので、
**画面の数字のほうが正しい**。差額は来館者の取り分になる。

数字を正直にした結果、入れた額より多く引き出す経路ができた。
そこでエンジンの被害額を 0 で止めるのをやめ、マイナス＝取り分として扱う。

判定画面は、これを素直に勝ちとして出す。

| | 表示 |
|---|---|
| 見出し | 詐欺師から奪った金額 |
| 金額 | **+¥21,400**（緑） |
| 口上 | 勝ち逃げ／おめでとうございます。あなたの勝ちです。 |

**勝ちは勝ちとして認める。** この経路では相手が本当に損をしているので、
「その気分こそが商品だった」と書くのは事実に反する。何も売りつけられていない。
最初はそう書いていたが、こじつけだった。

**本当の危険は、負けたことではなく勝ったこと。** 「勝てた」という喜びは、
そのまま「次もやれる」に変わる。相手が狙っているのは一回の21,400円ではなく、
次に自分から戻ってくることのほう。だから本文はこう運ぶ。

1. これは演出ではない。ここで引き上げれば、詐欺師に損をさせることは実際にできてしまう
2. 危ないのは勝ったとき。喜びが次の一戦を呼ぶ
3. **ここで勝って、勝ち逃げしてください。この一回で終わりにしてください**
4. 次に出会ったら相手にしない。「悪い奴だから奪ってやろう」とは考えない。
   手口は日々巧妙になる。勝った経験があるぶんだけ、次は深く入る
5. **勝ちの喜びにとらわれた人は、大負けするまで降りられません**

判定画面の口上からも「……気分は、いかがですか。」を外した。
読み手の感情を決めつけずに、起きた事実だけを置く。

被害額がマイナスになると壊れる箇所も直した。診断の被害率は 0 で止め、
合計被害額は黒字ぶんで他室の被害を相殺しないようにしてある。

### 3.6 公開し直したときの、開きっぱなしのページ

来館者の端末に、体験の最後でこれが出た。

```
Unexpected Application Error!
Failed to fetch dynamically imported module: .../assets/ResultPage-XXXX.js
Hey developer ...
```

画面ごとにJSを分けているのでファイル名にはハッシュが入る。公開し直すと
名前が全部変わり、そのとき開きっぱなしだったページは古い名前を握ったままになる。
判定画面へ移ろうとした瞬間に、もう存在しないファイルを取りに行って失敗した。
**体験を最後までやり遂げた人が、種明かしの手前で落ちる**という、いちばん悪い形で出る。

さらに SPA フォールバックが `/*  /index.html  200` だったため、消えたファイルにも
HTML が 200 で返っていた。ブラウザは HTML を JS として読もうとするので、
404 より分かりにくい失敗になる。

三つとも直した。

| | 直し方 |
|---|---|
| 復帰 | 読み込みに失敗したら一度だけページを読み込み直し、新しい版に乗り換える。進行は localStorage にあるので続きから見られる |
| 繰り返し防止 | 10秒以内の再試行はしない。読み込み直しても駄目なら、ネットワーク側の問題として案内画面に任せる |
| 案内画面 | ルートに `errorElement` を置いた。無いと React Router の開発者向け画面がそのまま来館者に出る |

配信側でも直そうとしたが、できなかった。`_redirects` に

```
/assets/*    /index.html   404
```

と書いて公開したところ、本番では無視された（存在しないファイルは変わらず 200 + HTML）。
Cloudflare Pages の `_redirects` が受け付ける状態コードは 301/302/303/307/308 と
書き換えの 200 だけで、404 は Netlify の機能。全ルートを列挙してフォールバックを
絞れば 404 にできるが、画面を足すたびに書き忘れるとその画面が本当に 404 になる。
失敗の見え方が少し綺麗になるのと引き換えにするには重いので、配信側では直さない。

HTML が 200 で返る場合でもモジュールとしては失敗するため、上の復帰で同じように拾える。
本番で、来館者が踏んだのと同じファイル名を読みに行かせ、エラー文が検出条件に
一致することを確認した。

検証は、本番ビルドを配信したうえでチャンクを実際に削除して行った。
消した状態では自動で読み込み直し、それでも無ければ日本語の案内画面。
戻せば従来どおり。

### 6.4 フォントのセルフホスト（2026-09-22）

LCP が 0.7〜2.6秒の幅で揺れていた。原因は Google Fonts で、
**返ってくる CSS が 145KB**（アプリ本体の JS 115KB より大きい）あり、
それがレンダリングを止めていた。5ウェイト × 約120分割の
`@font-face` 宣言が並ぶため。

素朴にセルフホストすると逆に重くなる（日本語は1ウェイト数MB）。
本文がビルド時に確定していることを使って、必要な文字だけ切り出した。

| | 収録範囲 | 字数 | サイズ |
|---|---|---|---|
| Noto Sans JP 400 / 700（本文） | 画面に出る全文字 | 1,509 | 246KB + 251KB |
| Zen Kaku Gothic New 700 / 900（見出し） | 見出しに出た漢字＋かな英数記号 | 630 | 59KB + 58KB |

見出し用を分けたのは、見出しに出る漢字が181字しかないため。
本文と同じ1,023字を入れると 117KB が 347KB になる。

**結果（各3回計測）**

| | 変更前 | 変更後 |
|---|---|---|
| Performance | 92 / 70 / 94 | 98 / 98 / 99 |
| LCP | 1.3s / 2.6s / 1.1s | 1.0s / 1.0s / 0.9s |
| CLS | 0.077 | 0 |

CLS が 0 になったのは予期していなかった。フォントが遅れて届くあいだ
代替書体で描画され、切り替わる瞬間に文字幅が変わってずれていた分。
preload で最初の描画に間に合うようになり、ずれ自体が起きなくなった。

**踏んだ落とし穴**

- Google Fonts の `text=` によるサブセット配信は、Zen Kaku Gothic New では
  黙って無視され、標準の分割が返る。1KB のファイルが出て初めて気づいた。
  原本から自分で切り出す方式に変えた
- Cloudflare Pages の `_headers` は、当たった規則を上書きではなく**連結**する。
  `/assets/*` と `/assets/romance/*` を両方書いたら `Cache-Control` が二重になり、
  避けたかった「ファイル名固定の画像が1年 immutable」が効いていた。
  重ならないよう拡張子で切り分けた
- ファイル名を固定したまま `immutable` にすると、文字を足して作り直したときに
  古い実体が残って豆腐になる。Vite に出力させてハッシュを付けることで解消した

**維持するための仕掛け**：本文に新しい漢字を足して `npm run build-fonts` を
忘れると、その字が豆腐になる。書いた本人はキャッシュで気づかないので、
`npm run check-fonts` が機械で検出し、`check-release` でも落ちる。

### 6.5 スクリーンリーダー向けの構造（2026-09-22）

実機は動かせていない。ただし実機テストで最初に指摘されるはずの欠陥は、
支援技術が読む対象＝アクセシビリティツリーを調べれば先に見つかる。4件あった。

| 欠陥 | 影響 | 対応 |
|---|---|---|
| **体験画面に読み上げ領域が無い** | 相手のメッセージが一切読み上げられない | 会話に `role="log" aria-live="polite" aria-relevant="additions"` |
| 話者が分からない | 左右の位置と色でしか区別していない | 各吹き出しに読み上げ専用の「自分：」「〈相手名〉：」 |
| `document.title` が変わらない | SPAなので遷移しても「いまどこか」が分からない | `AppShell` で画面ごとに設定し、遷移を読み上げ領域に流す |
| `<main>` とスキップリンクが無い | 毎回ナビを読み飛ばせない | `HallLayout` と `PhoneFrame` に `<main id="main">`、スキップリンクを追加 |

1件目が決定的だった。この展示は**中身のすべてが動的に届くテキスト**なので、
読み上げられないということは、展示が存在しないのと同じになる。
axe が黙っていたのは、`aria-live` の不在が「属性の誤り」ではなく
「必要な属性が無い」状態で、静的解析では意図を判定できないため。

入力中の三点リーダーは `aria-hidden` にした。読み上げる内容が無いのに
live 領域の中にあると、空の追加を拾わせてしまう。

**残っているのは「聞かないと分からないこと」だけ。**

- 読み上げの冗長さが実用に耐えるか（メッセージは2.6秒間隔で届く。
  読み上げが追いつかず詰まる可能性がある。絶叫モードは特に危ない）
- 日本語の読み上げ精度（`¥4,300,000`、`#9110`、`188` など）
- NVDA のブラウズモードでの挙動、iOS VoiceOver のジェスチャ操作

構造の問題を先に潰したので、実機で見る範囲はここまで絞れている。

---

## 7. 未決事項（要相談）

1. サイト名の確定（「だまされる博物館」「詐欺の解剖学」「ATTENTION MUSEUM」など）
2. ~~テーマ2を被害者視点／加害者視点のどちらで実装するか~~
   → **決定（2026-09-21）：3テーマとも被害者視点で統一する。** 加害者視点は学習効果が高い一方、
   手口を「実行する側の手順」として提示する構図になり、§5-3（再利用可能性の遮断）に抵触するため採用しない。
3. 公開範囲（一般公開／限定共有／研修用）— 公開なら監修者の確保を推奨
4. 統計・出典の具体的な引用元（警察庁「特殊詐欺認知・検挙状況」等）の確定
5. アクセス解析を入れるか（入れる場合も個人特定情報は取得しない方針）

---

## 8. 次のアクション

1. 本SPECのレビュー・修正（特に第2章のコアルールと第5章の倫理要件）
2. テーマ1のシナリオ本文を別ファイル `scenarios/01-romance.md` に日本語で執筆（実装より先）
3. フェーズ1の実装開始
